import React from 'react';
import './index.css';   

const Cycle9Button = (props) => {

    return(
        <div>
            <button className='button'>{props.name}</button>
        </div>
    )
}
export default Cycle9Button;
